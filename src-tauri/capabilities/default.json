{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core permissions (event listen/emit for the agent feed and migrations live-refresh).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
